name: your_app_name
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.5.4

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  http: ^1.2.1
  flutter_gemini: ^2.0.1  # ✅ OFFICIAL Gemini package
  flutter_dotenv: ^5.1.0      # ✅ Secure API key
  google_generative_ai: ^0.4.0  # ✅ OFFICIAL Google Gemini (NOT flutter_gemini)

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/Images/Chatbot-Logo.png